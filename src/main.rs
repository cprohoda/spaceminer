extern crate piston_window;
extern crate rand;

fn main() {
    let mut window: piston::PistonWindow = piston_window::WindowSettings::new("Space Miner", (960, 540))
        .exit_on_esc(true)
        .build()
        .unwrap_or_else(|error| {panic!("Build failed: ", error)});

    while let Some(event) = window.next() {
        match e {
            Input::
        }
    }
}